# AHMED-TECHINICAL-TASK

clone this REPO and update it https://github.com/Hondanx/AHMED-TECHINICAL-TASK.git

Change Nginx Port

Update the configuration so that Nginx runs on port 8000 instead of the default port 80.

Update index.html

Replace the content of the index.html file with:

THE-NEW-HTML-INDEX


Rebuild and Run

Rebuild the Docker image or restart the Docker Compose stack so that the changes take effect.

Verify that Nginx is accessible on port 8000.

Bonus: Auto-Restart

Configure Nginx to automatically restart if the container or host system restarts.


